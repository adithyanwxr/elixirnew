import React from "react";
import PageClient from "./page.client";

export const metadata = {
  title: "All Projects | Elixir Homes",
  description:
    "Explore all residential projects by Elixir Homes — including completed, ongoing, and upcoming developments designed with comfort, refined architecture, and functional living spaces.",
};

const page = () => {
  return <PageClient />;
};

export default page;
